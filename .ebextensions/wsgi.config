option_settings:
  - namespace: aws:elasticbeanstalk:container:python
    option_name: WSGIPath
    value: "application:app"

packages:
  yum:
    git: []

services:
  sysvinit:
    nginx:
      enabled: true
      ensureRunning: true
